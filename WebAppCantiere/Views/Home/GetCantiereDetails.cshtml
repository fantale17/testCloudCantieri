@model Lib.Cantiere

@{
    ViewData["Title"] = "GetCantiereDetails";
}

<h2>GetCantiereDetails</h2>

<div>
    <h4>Cantiere</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NomeCliente)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.NomeCliente)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.EmailCliente)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.EmailCliente)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Luogo)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Luogo)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Id)
        </dd>
    </dl>
</div>
<div class="row">
    <p>Photos:</p>
    @if (Model.UriPhotos != null && Model.UriPhotos.Count > 0)
    {
        foreach (var uri in Model.UriPhotos)
         {
             <img src="@uri"/>
         }
    }
</div>
<div>
    @Html.ActionLink("Create Intervento", "CreateIntervento", new {  idCantiere = Model.Id}) |
    <a asp-action="Index">Back to List</a>
</div>
